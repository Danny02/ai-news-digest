# Copy to .dev.vars and fill in real values. Never commit .dev.vars.
RESEND_API_KEY=
SENDER=AI News Digest <digest@nullzwo.dev>

# The segment ids are not secrets. wrangler dev reads them from [vars] in
# wrangler.toml; repeating them here would shadow that and go stale.
