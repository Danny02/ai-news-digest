# Copy to .dev.vars and fill in real values. Never commit .dev.vars.
RESEND_API_KEY=
SENDER=AI News Digest <digest@nullzwo.dev>

# DAILY_SEGMENT_ID and WEEKLY_SEGMENT_ID are not secrets. wrangler dev reads
# them from [vars] in wrangler.toml; repeating them here would shadow that
# value and go stale.
